<?xml version="1.0" encoding="utf-8"?>
<!--
  The manifest every Hive app carries. Only package, label, icon and version
  differ between one app and the next, which is why the shim it names can be
  built once and reused: an activity's Java package and an application's id are
  allowed to disagree.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="hive.chat"
	android:versionCode="1"
	android:versionName="1">

	<uses-sdk android:minSdkVersion="24" android:targetSdkVersion="35" />

	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

	<application
		android:label="chat"
		android:icon="@mipmap/icon"
		android:hasCode="true"
		android:extractNativeLibs="true"
		android:usesCleartextTraffic="true">

		<activity
			android:name="dev.hive.app.MainActivity"
			android:exported="true"
			android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
			android:windowSoftInputMode="adjustResize"
			android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|keyboard|density|fontScale|uiMode|locale|navigation|touchscreen">

			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>

		</activity>

	</application>

</manifest>
